<?xml version="1.0" encoding="UTF-8"?>
<features name="org.ops4j.pax.jpa-${project.version}" xmlns="http://karaf.apache.org/xmlns/features/v1.0.0">

    <repository>mvn:org.ops4j.pax.jpa/pax-jpa-features/${project.version}/xml/features</repository>

    <feature name="pax-jpa-sample1" description="Install PAX JPA Sample1" version="${project.version}" resolver="(obr)">
        <feature>pax-jpa</feature>

        <bundle>mvn:org.ops4j.pax.jpa.samples/pax-jpa-sample1/${project.version}</bundle>
    </feature>

    <feature name="pax-jpa-sample2" description="Install PAX JPA Sample2" version="${project.version}" resolver="(obr)">
        <feature>pax-jpa</feature>

        <bundle>mvn:org.ops4j.pax.jpa.samples/pax-jpa-sample2/${project.version}</bundle>
    </feature>
    <feature name="pax-jpa-sample3" description="Install PAX JPA Sample3" version="${project.version}" resolver="(obr)">
        <feature>pax-jpa</feature>

        <bundle>mvn:org.ops4j.pax.jpa.samples/pax-jpa-sample3/${project.version}</bundle>
    </feature>

</features>
